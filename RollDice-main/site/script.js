function rolar_dado(){
    return Math.floor(math.random() * 21);
}
function mostrar_dado(){
    const resultado = rolar_dado();
    document.window.getElementById(resultado)
}
const dado = 
function soma(){
    var variavel = window.document.getElementById(num1)
    var somatorio = Number(num1)

}